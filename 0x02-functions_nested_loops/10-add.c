#include "main.h"

/**
* add - Adds two integers and returns the result
*@a: takes the number for addition
*@b: takes the number for addition
* Return: Nothing
*/
int add(int a, int b)
{
	return (a + b);
}
